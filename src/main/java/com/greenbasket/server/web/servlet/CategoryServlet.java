/*
HTTP-слой для категорий.

Что внутри:

Аналог ProductServlet, но работает с CategoryService.

Возможна ручка GET /{id}/products (позже).

Что изучить: Те же темы.

Что можно дописать:

Возврат категорий с вложенными продуктами (опционально, лучше — отдельным запросом).

Массовые операции (bulk create/update).
 */
package com.greenbasket.web.servlet;