/*
Единая точка перехвата исключений → стандартизированный JSON с ошибкой.

Что внутри:

doFilter с try/catch.

В catch — HTTP статус (обычно 400/404/500), JSON-ответ вида {"error":"..."}.

(Опционально) логирование stacktrace.

Что изучить: Цепочка фильтров, семантика HTTP-статусов, дизайн error-ответов.

Что можно дописать:

Разные статусы по типу исключения (VALIDATION → 400, NOT_FOUND → 404).

Корреляционный X-Request-Id в ответах.

Маскирование деталей SQL-ошибок.
 */
package com.greenbasket.web.filter;